meta {
  name: Update My Tenant
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/tenants/me
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Novo Nome do Escritório"
  }
}
